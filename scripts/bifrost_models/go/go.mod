// Module path's last segment becomes the binary name produced by
// buildGoModule when building a single-package main. Keep this as
// `bifrost-models` so the resulting executable matches.
module bifrost-models

go 1.22
