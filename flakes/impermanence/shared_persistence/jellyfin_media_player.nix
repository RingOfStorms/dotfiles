# Jellyfin Media Player: server list, user login, playback prefs.
{
  system = {
    directories = [ ];
    files = [ ];
  };
  user = {
    directories = [
      ".local/share/jellyfin-desktop"
      ".config/jellyfinmediaplayer"
    ];
    files = [ ];
  };
}
