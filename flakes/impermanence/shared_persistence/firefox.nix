# Mozilla Firefox user profile (cookies, logins, extensions, sessions, etc.).
{
  system = {
    directories = [ ];
    files = [ ];
  };
  user = {
    directories = [ ".mozilla" ];
    files = [ ];
  };
}
