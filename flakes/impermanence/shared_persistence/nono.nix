# nono sandbox runner: profile definitions, models.toml, and the
# checked-in profiles git repo under ~/.config/nono.
{
  system = {
    directories = [ ];
    files = [ ];
  };
  user = {
    directories = [ ".config/nono" ];
    files = [ ];
  };
}
