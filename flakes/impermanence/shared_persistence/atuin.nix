# Atuin shell history (config + the synced history db).
{
  system = {
    directories = [ ];
    files = [ ];
  };
  user = {
    directories = [
      ".config/atuin"
      ".local/share/atuin"
    ];
    files = [ ];
  };
}
