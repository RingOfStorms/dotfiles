# Google Chrome user profile (cookies, extensions, sessions, etc.).
{
  system = {
    directories = [ ];
    files = [ ];
  };
  user = {
    directories = [ ".config/google-chrome" ];
    files = [ ];
  };
}
