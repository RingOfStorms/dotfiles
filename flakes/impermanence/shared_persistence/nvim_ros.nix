# State dir for the ros_neovim distro (shada, swap, undo, plugin data).
{
  system = {
    directories = [ ];
    files = [ ];
  };
  user = {
    directories = [ ".local/state/nvim_ringofstorms_helium" ];
    files = [ ];
  };
}
