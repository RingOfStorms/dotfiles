# opencode (CLI agent) config, project caches, and per-session state.
{
  system = {
    directories = [ ];
    files = [ ];
  };
  user = {
    directories = [
      ".config/opencode"
      ".local/share/opencode"
      ".local/state/opencode"
    ];
    files = [ ];
  };
}
