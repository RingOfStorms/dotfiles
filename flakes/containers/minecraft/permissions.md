> Just list of perms currently used, these need to be manually run on a new server.

## Survival

```
/lp group default permission set deathchest.place true server=survival
```

## Creative

```
/lp group default permission set worldedit.* true server=creative
/lp group default permission set essentials.* true server=creative
/lp group default permission set fawe.* true server=creative
```
